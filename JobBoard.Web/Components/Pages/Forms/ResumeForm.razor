@using JobBoard.Web.Services
@using JobBoard.Web.Utils
@inject ResumeApiClient ResumeApi
@inject IJSRuntime JSRuntime

@rendermode InteractiveServer

<div class="modal fade" id="resumeModal" aria-labelledby="resumeModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content rounded-4 shadow">
            <div class="modal-header pb-0">
                <h5 class="modal-title fw-bold" id="resumeModalLabel">
                    <i class="bi bi-file-text-fill me-2"></i>@(IsEdit ? "Edit Resume" : "Create Resume")
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body pt-0">
                <div class="row g-3">
                    <div class="col-md-4">
                        <label class="form-label fw-semibold text-secondary" for="FullName">Full Name</label>
                        <input type="text" class="form-control rounded-3" id="FullName" name="FullName" @bind="model.FullName" />
                    </div>
                    <div class="col-md-4">
                        <label class="form-label fw-semibold text-secondary" for="Title">Headline / Title</label>
                        <input type="text" class="form-control rounded-3" id="Title" name="Title" @bind="model.Title" />
                    </div>
                    <div class="col-md-4">
                        <label class="form-label fw-semibold text-secondary" for="Location">Location</label>
                        <input type="text" class="form-control rounded-3" id="Location" name="Location" placeholder="City, Country" @bind="model.Location" />
                    </div>
                </div>

                <div class="row mt-3">
                    <div class="col-md-6">
                        <label class="form-label fw-semibold text-secondary" for="ExpectedSalary">Expected Salary (USD)</label>
                        <input type="number" class="form-control rounded-3" id="ExpectedSalary" name="ExpectedSalary" @bind="model.ExpectedSalary" step="1000" placeholder="e.g., 80000" />
                    </div>
                </div>

                <div class="mt-3">
                    <label class="form-label fw-semibold text-secondary" for="AboutMe">About Me</label>
                    <textarea class="form-control rounded-3" id="AboutMe" name="AboutMe" rows="4" @bind="model.AboutMe"></textarea>
                    <div class="form-check mt-2 mb-4">
                        <input type="checkbox" class="form-check-input rounded-3" id="isVisibleCheck" name="IsVisible" @bind="model.IsVisible" />
                        <label class="form-check-label text-secondary" for="isVisibleCheck">
                            <i class="bi bi-eye me-1"></i> Visible to employers
                        </label>
                    </div>
                </div>

                <hr class="my-4" />

                <h5 class="fw-bold mb-3">
                    <i class="bi bi-envelope-paper me-2"></i>Contact Methods
                </h5>
                <div class="mb-4">
                    @for (int i = 0; i < model.ContactMethods.Count; i++)
                    {
                        var contact = model.ContactMethods[i];
                        <div class="row mb-3">
                            <div class="col-md-4">
                                <label class="form-label fw-semibold text-secondary" for="ContactMethods_@(i)_Type">Type</label>
                                <select class="form-select rounded-3" id="ContactMethods_@(i)_Type" name="ContactMethods[@i].Type" @bind="contact.Type">
                                    <option value="">-- Select --</option>
                                    <option value="Email">Email</option>
                                    <option value="Phone">Phone</option>
                                    <option value="LinkedIn">LinkedIn</option>
                                    <option value="GitHub">GitHub</option>
                                    <option value="Website">Website</option>
                                </select>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label fw-semibold text-secondary" for="ContactMethods_@(i)_Value">Value</label>
                                <input type="text" class="form-control rounded-3" id="ContactMethods_@(i)_Value" name="ContactMethods[@i].Value" placeholder="Value" @bind="contact.Value" />
                            </div>
                            <div class="col-md-2 d-flex align-items-end">
                                <button type="button" class="btn btn-outline-danger rounded-3 w-100 d-inline-flex align-items-center justify-content-center px-3" @onclick="() => RemoveContact(contact)">
                                    <i class="bi bi-trash me-1"></i> <span class="d-md-none d-lg-inline">Remove</span>
                                </button>
                            </div>
                        </div>
                    }
                    <button type="button" class="btn btn-sm btn-outline-primary rounded-pill mt-2 px-3" @onclick="AddContact">
                        <i class="bi bi-plus-circle me-1"></i> Add Contact
                    </button>
                </div>

                <h5 class="fw-bold mb-3">
                    <i class="bi bi-chat-dots me-2"></i>Languages
                </h5>
                <div class="mb-4">
                    @for (int i = 0; i < model.Languages.Count; i++)
                    {
                        var lang = model.Languages[i];
                        <div class="row mb-3">
                            <div class="col-md-5">
                                <label class="form-label fw-semibold text-secondary" for="Languages_@(i)_Name">Language</label>
                                <select class="form-select rounded-3" id="Languages_@(i)_Name" name="Languages[@i].Name" @bind="lang.Name">
                                    <option value="">-- Select Language --</option>
                                    @foreach (var langOption in GetAvailableLanguages(lang.Name))
                                    {
                                        <option value="@langOption">@langOption</option>
                                    }
                                </select>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label fw-semibold text-secondary" for="Languages_@(i)_Level">Level</label>
                                <select class="form-select rounded-3" id="Languages_@(i)_Level" name="Languages[@i].Level" @bind="lang.Level">
                                    <option value="">-- Level --</option>
                                    <option value="A1">A1</option>
                                    <option value="A2">A2</option>
                                    <option value="B1">B1</option>
                                    <option value="B2">B2</option>
                                    <option value="C1">C1</option>
                                    <option value="C2">C2</option>
                                    <option value="Native">Native</option>
                                </select>
                            </div>
                            <div class="col-md-3 d-flex align-items-end">
                                <button type="button" class="btn btn-outline-danger rounded-3 w-100 d-inline-flex align-items-center justify-content-center px-3" @onclick="() => RemoveLanguage(lang)">
                                    <i class="bi bi-trash me-1"></i> Remove
                                </button>
                            </div>
                        </div>
                    }
                    <button type="button" class="btn btn-sm btn-outline-primary rounded-pill mt-2 px-3" @onclick="AddLanguage">
                        <i class="bi bi-plus-circle me-1"></i> Add Language
                    </button>
                </div>

                <h5 class="fw-bold mb-3">
                    <i class="bi bi-tools me-2"></i>Frameworks & Tools
                </h5>
                <div class="mb-4">
                    <label class="form-label fw-semibold text-secondary" for="newFramework">Framework/Tool</label>
                    <div class="input-group">
                        <input type="text"
                               class="form-control rounded-start-3"
                               id="newFramework"
                               name="newFramework"
                               placeholder="Type framework and press Enter"
                               @bind="newFramework"
                               @bind:event="oninput"
                               @onkeypress="OnFrameworkKeyPress" />
                        <button class="btn btn-outline-primary rounded-end-3 px-4" type="button" @onclick="AddFramework">
                            <i class="bi bi-plus"></i> Add
                        </button>
                    </div>
                    <div class="mt-2 d-flex flex-wrap gap-2">
                        @foreach (var fw in model.Frameworks)
                        {
                            <span class="badge bg-info text-dark rounded-pill p-2 px-3 d-flex align-items-center gap-2">
                                @fw
                                <button type="button" class="btn-close ms-1" @onclick="() => RemoveFramework(fw)"></button>
                            </span>
                        }
                    </div>
                </div>

                <hr class="my-4" />

                <h5 class="fw-bold mb-3">
                    <i class="bi bi-briefcase me-2"></i>Work Experience
                </h5>
                @for (int i = 0; i < model.WorkExperiences.Count; i++)
                {
                    var we = model.WorkExperiences[i];
                    <div class="card mb-4 border-1 shadow-sm rounded-4">
                        <div class="card-body p-4">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-semibold text-secondary" for="WorkExperiences_@(i)_CompanyName">Company</label>
                                    <input type="text" class="form-control rounded-3" id="WorkExperiences_@(i)_CompanyName" name="WorkExperiences[@i].CompanyName" @bind="we.CompanyName" />
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-semibold text-secondary" for="WorkExperiences_@(i)_Position">Position</label>
                                    <input type="text" class="form-control rounded-3" id="WorkExperiences_@(i)_Position" name="WorkExperiences[@i].Position" @bind="we.Position" />
                                </div>
                            </div>
                            <div class="row g-3 mt-1">
                                <div class="col-md-6">
                                    <label class="form-label fw-semibold text-secondary" for="WorkExperiences_@(i)_StartMonthYear">Start Date</label>
                                    <input type="month" class="form-control rounded-3" id="WorkExperiences_@(i)_StartMonthYear" name="WorkExperiences[@i].StartMonthYear" @bind="we.StartMonthYear" max="@GetCurrentYearMonth()" />
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-semibold text-secondary" for="WorkExperiences_@(i)_EndMonthYear">End Date (leave blank if current)</label>
                                    <input type="month" class="form-control rounded-3" id="WorkExperiences_@(i)_EndMonthYear" name="WorkExperiences[@i].EndMonthYear" @bind="we.EndMonthYear" max="@GetCurrentYearMonth()" />
                                </div>
                            </div>
                            <div class="mt-3">
                                <label class="form-label fw-semibold text-secondary" for="WorkExperiences_@(i)_NewTech">Technologies Used</label>
                                <div class="input-group mb-2">
                                    <input type="text"
                                           class="form-control rounded-start-3"
                                           id="WorkExperiences_@(i)_NewTech"
                                           name="WorkExperiences[@i].NewTech"
                                           placeholder="Type tech (e.g. Docker) and press Enter"
                                           @bind="we.NewTech"
                                           @bind:event="oninput"
                                           @onkeydown="(e) => OnTechKeyPress(e, we)" />
                                    <button class="btn btn-outline-primary rounded-end-3 px-3" type="button" @onclick="() => AddTech(we)">
                                        <i class="bi bi-plus"></i> Add
                                    </button>
                                </div>
                                <div class="d-flex flex-wrap gap-2">
                                    @foreach (var tech in we.Technologies)
                                    {
                                        <span class="badge bg-secondary rounded-pill p-2 px-3 d-flex align-items-center gap-2">
                                            @tech
                                            <button type="button" class="btn-close btn-close-white ms-1" @onclick="() => RemoveTech(we, tech)"></button>
                                        </span>
                                    }
                                </div>
                            </div>
                            <div class="mt-3">
                                <label class="form-label fw-semibold text-secondary" for="WorkExperiences_@(i)_Description">Description</label>
                                <textarea class="form-control rounded-3" rows="3" id="WorkExperiences_@(i)_Description" name="WorkExperiences[@i].Description" @bind="we.Description"></textarea>
                            </div>
                            <div class="mt-3 text-end">
                                <button class="btn btn-outline-danger rounded-3 px-4 d-inline-flex align-items-center" @onclick="() => RemoveWorkExperience(we)">
                                    <i class="bi bi-trash me-2"></i> Remove Role
                                </button>
                            </div>
                        </div>
                    </div>
                }
                <div class="text-center mb-4">
                    <button type="button" class="btn btn-outline-primary rounded-pill px-4" @onclick="AddWorkExperience">
                        <i class="bi bi-plus-circle me-2"></i> Add Work Experience
                    </button>
                </div>

                @if (!string.IsNullOrEmpty(ErrorMessage))
                {
                    <div class="alert alert-danger rounded-3 mt-3">@ErrorMessage</div>
                }
            </div>
            <div class="modal-footer pt-0 pb-4 justify-content-center">
                <button type="button" class="btn btn-light rounded-pill px-5 me-2" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-primary rounded-pill px-5" @onclick="Save" disabled="@isSaving">
                    @(isSaving ? "Saving..." : "Save Profile")
                </button>
            </div>
        </div>
    </div>
</div>

@code {
    [Parameter] public ResumeResponse? ExistingResume { get; set; }
    [Parameter] public EventCallback OnSuccess { get; set; }

    private ResumeFormModel model = new();
    private bool isSaving;
    private string? ErrorMessage;
    private bool IsEdit => ExistingResume != null;

    private string newFramework = "";

    protected override void OnParametersSet()
    {
        if (IsEdit && ExistingResume != null)
        {
            model = new ResumeFormModel
            {
                Id = ExistingResume.Id,
                FullName = ExistingResume.FullName,
                Title = ExistingResume.Title,
                Location = ExistingResume.Location,
                ExpectedSalary = ExistingResume.ExpectedSalary,
                AboutMe = ExistingResume.AboutMe,
                IsVisible = ExistingResume.IsVisible,
                ContactMethods = ExistingResume.ContactMethods.Select(c => new ContactMethodDto { Type = c.Type, Value = c.Value }).ToList(),
                Languages = ExistingResume.Skills.Languages.Select(l => new LanguageDto { Name = l.Name, Level = l.Level }).ToList(),
                Frameworks = ExistingResume.Skills.HardSkills.ToList(),
                WorkExperiences = ExistingResume.WorkExperiences.Select(we => new WorkExperienceFormModel
                {
                    Id = we.Id,
                    CompanyName = we.CompanyName,
                    Position = we.Position,
                    StartDate = we.StartDate,
                    EndDate = we.EndDate,
                    Description = we.Description,
                    Technologies = we.Technologies.ToList()
                }).ToList()
            };
        }
        else
        {
            model = new();
        }
    }

    private IEnumerable<string> GetAvailableLanguages(string currentLanguage)
    {
        var selectedNames = model.Languages
            .Select(l => l.Name)
            .Where(n => n != currentLanguage) // Don't filter out the one I'm currently using
            .ToHashSet();

        return LanguageData.AllLanguageNames.Where(lang => !selectedNames.Contains(lang));
    }

    private void AddTech(WorkExperienceFormModel we)
    {
        var trimmed = we.NewTech?.Trim();

        if (!string.IsNullOrWhiteSpace(trimmed))
        {
            bool alreadyExists = we.Technologies
                .Any(t => string.Equals(t, trimmed, StringComparison.OrdinalIgnoreCase));

            if (!alreadyExists)
            {
                we.Technologies.Add(trimmed);
            }
        }

        we.NewTech = ""; // Clear input regardless of success/duplicate
    }

    private void OnTechKeyPress(KeyboardEventArgs e, WorkExperienceFormModel we)
    {
        if (e.Key == "Enter")
        {
            AddTech(we);
        }
    }

    private void RemoveTech(WorkExperienceFormModel we, string tech)
    {
        we.Technologies.Remove(tech);
    }

    private string GetCurrentYearMonth() => DateTime.Now.ToString("yyyy-MM");

    // Contact methods
    private void AddContact()
    {
        model.ContactMethods.Add(new ContactMethodDto { Type = "", Value = "" });
    }

    private void RemoveContact(ContactMethodDto contact)
    {
        model.ContactMethods.Remove(contact);
    }

    // Languages
    private void AddLanguage()
    {
        model.Languages.Add(new LanguageDto { Name = "", Level = "" });
    }

    private void RemoveLanguage(LanguageDto lang)
    {
        model.Languages.Remove(lang);
    }

    private void AddFramework()
    {
        var trimmed = newFramework?.Trim();

        if (!string.IsNullOrWhiteSpace(trimmed))
        {
            // Case-insensitive check: prevents duplicates like "React" and "react"
            bool alreadyExists = model.Frameworks
                .Any(f => string.Equals(f, trimmed, StringComparison.OrdinalIgnoreCase));

            if (!alreadyExists)
            {
                model.Frameworks.Add(trimmed);
            }
        }

        // Clear the input even if it was a duplicate to reset the UI
        newFramework = "";
    }

    private void OnFrameworkKeyPress(KeyboardEventArgs e)
    {
        if (e.Key == "Enter")
        {
            AddFramework();
            StateHasChanged();
        }
    }

    private void RemoveFramework(string fw)
    {
        model.Frameworks.Remove(fw);
    }

    // Work Experience
    private void AddWorkExperience()
    {
        model.WorkExperiences.Add(new WorkExperienceFormModel
        {
            StartDate = DateOnly.FromDateTime(DateTime.Now),
            EndDate = null
        });
    }

    private void RemoveWorkExperience(WorkExperienceFormModel we)
    {
        model.WorkExperiences.Remove(we);
    }

    private async Task Save()
    {
        isSaving = true;
        ErrorMessage = null;

        try
        {
            var skillsDto = new SkillsMatrixDto(
                model.Languages,
                model.Frameworks
            );

            var workExperiencesDto = model.WorkExperiences.Select(we => new WorkExperienceDto(
                we.Id,
                we.CompanyName,
                we.Position,
                we.StartDate,
                we.EndDate,
                we.Description,
                we.Technologies
            )).ToList();

            HttpResponseMessage response;
            if (IsEdit && model.Id.HasValue)
            {
                var updateRequest = new UpdateResumeRequest(
                    model.Id.Value,
                    model.FullName,
                    model.Title,
                    model.Location,
                    model.ExpectedSalary,
                    model.ContactMethods,
                    skillsDto,
                    model.IsVisible,
                    model.AboutMe,
                    workExperiencesDto
                );
                response = await ResumeApi.UpdateAsync(model.Id.Value, updateRequest);
            }
            else
            {
                var createRequest = new CreateResumeRequest(
                    model.FullName,
                    model.Title,
                    model.Location,
                    model.ExpectedSalary,
                    model.ContactMethods,
                    skillsDto,
                    model.IsVisible,
                    model.AboutMe,
                    workExperiencesDto
                );
                response = await ResumeApi.CreateAsync(createRequest);
            }

            if (response.IsSuccessStatusCode)
            {
                await OnSuccess.InvokeAsync();
                await CloseModal();
            }
            else
            {
                var error = await response.Content.ReadAsStringAsync();
                ErrorMessage = $"Error: {error}";
            }
        }
        catch (Exception ex)
        {
            ErrorMessage = $"Unexpected error: {ex.Message}";
        }
        finally
        {
            isSaving = false;
        }
    }

    private async Task CloseModal()
    {
        await JSRuntime.InvokeVoidAsync("hideModal", "#resumeModal");
    }

    public class ResumeFormModel
    {
        public Guid? Id { get; set; }
        public string FullName { get; set; } = "";
        public string Title { get; set; } = "";
        public string Location { get; set; } = "";
        public decimal? ExpectedSalary { get; set; } = 0;
        public string AboutMe { get; set; } = "";
        public bool IsVisible { get; set; } = true;

        public List<ContactMethodDto> ContactMethods { get; set; } = new();
        public List<LanguageDto> Languages { get; set; } = new();
        public List<string> Frameworks { get; set; } = new();
        public List<WorkExperienceFormModel> WorkExperiences { get; set; } = new();
    }

    public class WorkExperienceFormModel
    {
        public Guid? Id { get; set; }
        public string CompanyName { get; set; } = "";
        public string Position { get; set; } = "";
        public DateOnly StartDate { get; set; } = DateOnly.FromDateTime(DateTime.Now);
        public DateOnly? EndDate { get; set; }
        public string Description { get; set; } = "";
        public List<string> Technologies { get; set; } = new();

        // New property to track the text input for THIS specific work experience
        public string NewTech { get; set; } = "";

        public DateTime? StartMonthYear
        {
            get => new DateTime(StartDate.Year, StartDate.Month, 1);
            set => StartDate = value.HasValue ? DateOnly.FromDateTime(value.Value) : DateOnly.FromDateTime(DateTime.Now);
        }

        public DateTime? EndMonthYear
        {
            get => EndDate.HasValue ? new DateTime(EndDate.Value.Year, EndDate.Value.Month, 1) : null;
            set => EndDate = value.HasValue ? DateOnly.FromDateTime(value.Value) : null;
        }
    }
}